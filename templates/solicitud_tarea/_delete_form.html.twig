<form method="post" action="{{ path('solicitud_tarea_delete', {'id': solicitud_tarea.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ solicitud_tarea.id) }}">
    <button class="btn">Delete</button>
</form>