<form method="post" action="{{ path('area_coordinacion_delete', {'id': area_coordinacion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ area_coordinacion.id) }}">
    <button class="btn">Delete</button>
</form>